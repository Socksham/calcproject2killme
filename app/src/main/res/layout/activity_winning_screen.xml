<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    tools:context=".WinningScreen">

    <TextView
        android:id="@+id/winnerLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="You Won!"
        android:textColor="@color/white"
        android:textSize="48sp"
        android:fontFamily="serif-monospace"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="175dp"/>

    <TextView
        android:id="@+id/descLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Good job soldier, it's because of you that you and your team was able to survive that, because of you, humanity can see the light of tomorrow"
        android:gravity="center_horizontal"
        android:textColor="@color/white"
        android:fontFamily="serif-monospace"
        android:textSize="20sp"
        android:layout_below="@id/winnerLabel"/>

    <Button
        android:id="@+id/startOverButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/winnerLabel"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="200dp"
        android:background="@drawable/main_custom_button"
        android:minWidth="200dp"
        android:text="Return to Menu"
        android:textAllCaps="false"
        android:textSize="30sp" />

</RelativeLayout>